# -----------------------------------------
# ОБЩИЙ ФАЙЛ КОНФИГУРАЦИИ МИКРОСЕРВИСНОЙ АРХИТЕКТУРЫ
# -----------------------------------------
# Этот файл содержит все переменные окружения для всех сервисов
# с префиксами для избежания конфликтов имен.
# -----------------------------------------

GENERAL_OTLP_HOST=localhost
GENERAL_OTLP_PORT=4317

# -----------------------------------------
# CORE НАСТРОЙКИ (ОБЩИЕ КОМПОНЕНТЫ)
# -----------------------------------------

# Kafka настройки
CORE_KAFKA_EXTERNAL_PORT=9092
CORE_KAFKA_INTERNAL_PORT=29092
CORE_KAFKA_CONTROLLER_PORT=29093
CORE_KAFKA_UI_PORT=8090

# OTEL настройки
CORE_OTEL_IMAGE_NAME=otel/opentelemetry-collector-contrib:0.123.0
CORE_OTEL_CONTAINER_NAME=otel-collector
CORE_OTEL_EXTERNAL_GRPC_PORT=4317
CORE_OTEL_INTERNAL_GRPC_PORT=4317
CORE_OTEL_EXTERNAL_HTTP_PORT=8888
CORE_OTEL_INTERNAL_HTTP_PORT=8888

# Elasticsearch настройки
CORE_ELASTICSEARCH_IMAGE_NAME=docker.elastic.co/elasticsearch/elasticsearch:9.0.4
CORE_ELASTICSEARCH_CONTAINER_NAME=elasticsearch
CORE_ELASTICSEARCH_EXTERNAL_HTTP_PORT=9200
CORE_ELASTICSEARCH_INTERNAL_HTTP_PORT=9200

# Kibana настройки
CORE_KIBANA_IMAGE_NAME=docker.elastic.co/kibana/kibana:9.0.4
CORE_KIBANA_CONTAINER_NAME=kibana
CORE_KIBANA_EXTERNAL_HTTP_PORT=5601
CORE_KIBANA_INTERNAL_HTTP_PORT=5601

# Prometheus настроки
CORE_PROMETHEUS_IMAGE_NAME=prom/prometheus:v3.3.1
CORE_PROMETHEUS_CONTAINER_NAME=prometheus
CORE_PROMETHEUS_EXTERNAL_HTTP_PORT=9090
CORE_PROMETHEUS_INTERNAL_HTTP_PORT=9090

# Grafana настроки
CORE_GRAFANA_IMAGE_NAME=grafana/grafana:12.0.0
CORE_GRAFANA_CONTAINER_NAME=grafana
CORE_GRAFANA_EXTERNAL_HTTP_PORT=3000
CORE_GRAFANA_INTERNAL_HTTP_PORT=3000

# Jaeger настроки
CORE_JAEGER_IMAGE_NAME=jaegertracing/jaeger:2.6.0
CORE_JAEGER_CONTAINER_NAME=jaeger
CORE_JAEGER_INTERNAL_GRPC_PORT=4317
CORE_JAEGER_EXTERNAL_GRPC_PORT=4317
CORE_JAEGER_UI_EXTERNAL_PORT=16686

# -----------------------------------------
# IAM СЕРВИС
# -----------------------------------------

# gRPC настройки
IAM_GRPC_HOST=localhost
IAM_GRPC_PORT=50053

# Логгер
IAM_LOGGER_LEVEL=info
IAM_LOGGER_AS_JSON=true
IAM_LOGGER_SERVICE_NAME=iam-service
IAM_LOGGER_SERVICE_ENVIRONMENT=dev
IAM_LOGGER_ENABLE_OTLP=true

# PostgreSQL
IAM_POSTGRES_HOST=localhost
IAM_POSTGRES_PORT=5444
IAM_EXTERNAL_POSTGRES_PORT=5444
IAM_POSTGRES_USER=iam_user
IAM_POSTGRES_PASSWORD=iam_password
IAM_POSTGRES_DB=iam
IAM_POSTGRES_SSL_MODE=disable
IAM_MIGRATION_DIRECTORY=./migrations

# Redis
IAM_REDIS_HOST=localhost
IAM_REDIS_PORT=6333
IAM_EXTERNAL_REDIS_PORT=6333
IAM_REDIS_CONNECTION_TIMEOUT=10s
IAM_REDIS_MAX_IDLE=10
IAM_REDIS_IDLE_TIMEOUT=10s

# Сессии
IAM_SESSION_TTL=24h

# -----------------------------------------
# INVENTORY СЕРВИС
# -----------------------------------------

# gRPC сервер
INVENTORY_GRPC_PORT=50051
INVENTORY_GRPC_HOST=0.0.0.0

# gRPC клиенты
INVENTORY_IAM_GRPC_HOST=localhost
INVENTORY_IAM_GRPC_PORT=50053

# Логгер
INVENTORY_LOGGER_LEVEL=info
INVENTORY_LOGGER_AS_JSON=true
INVENTORY_LOGGER_SERVICE_NAME=inventory-service
INVENTORY_LOGGER_SERVICE_ENVIRONMENT=dev
INVENTORY_LOGGER_ENABLE_OTLP=true

# MongoDB
INVENTORY_MONGO_IMAGE_NAME=mongo:7.0.5
INVENTORY_EXTERNAL_MONGO_PORT=27018
INVENTORY_MONGO_HOST=localhost
INVENTORY_MONGO_PORT=27018
INVENTORY_MONGO_AUTH_DB=admin
INVENTORY_MONGO_INITDB_DATABASE=inventory-service
INVENTORY_MONGO_INITDB_ROOT_USERNAME=inventory-service-user
INVENTORY_MONGO_INITDB_ROOT_PASSWORD=inventory-service-password

# -----------------------------------------
# ORDER СЕРВИС
# -----------------------------------------

# gRPC клиенты
ORDER_INVENTORY_GRPC_HOST=localhost
ORDER_INVENTORY_GRPC_PORT=50051
ORDER_PAYMENT_GRPC_HOST=localhost
ORDER_PAYMENT_GRPC_PORT=50052
ORDER_IAM_GRPC_HOST=localhost
ORDER_IAM_GRPC_PORT=50053

# http сервер
ORDER_HTTP_HOST=0.0.0.0
ORDER_HTTP_PORT=8080
ORDER_HTTP_READ_TIMEOUT=5s

# Kafka настройки
ORDER_KAFKA_BROKERS=localhost:9092
ORDER_ORDER_PAID_TOPIC_NAME=order.paid
ORDER_ORDER_ASSEMBLED_TOPIC_NAME=order.assembled
ORDER_ORDER_ASSEMBLED_CONSUMER_GROUP_ID=order-group-order-assembled

# Логгер
ORDER_LOGGER_LEVEL=info
ORDER_LOGGER_AS_JSON=true
ORDER_LOGGER_SERVICE_NAME=order-service
ORDER_LOGGER_SERVICE_ENVIRONMENT=dev
ORDER_LOGGER_ENABLE_OTLP=true

# PostgreSQL
ORDER_POSTGRES_IMAGE_NAME=postgres:15
ORDER_EXTERNAL_POSTGRES_PORT=5435
ORDER_POSTGRES_HOST=localhost
ORDER_POSTGRES_PORT=5435
ORDER_POSTGRES_USER=order-service-user
ORDER_POSTGRES_PASSWORD=order-service-password
ORDER_POSTGRES_DB=order-service
ORDER_POSTGRES_SSL_MODE=disable
ORDER_MIGRATION_DIRECTORY=./migrations

# -----------------------------------------
# PAYMENT СЕРВИС
# -----------------------------------------

# gRPC сервер
PAYMENT_GRPC_HOST=0.0.0.0
PAYMENT_GRPC_PORT=50052

# Логгер
PAYMENT_LOGGER_LEVEL=info
PAYMENT_LOGGER_AS_JSON=true
PAYMENT_LOGGER_SERVICE_NAME=payment-service
PAYMENT_LOGGER_SERVICE_ENVIRONMENT=dev
PAYMENT_LOGGER_ENABLE_OTLP=true

# -----------------------------------------
# ASSEMBLY СЕРВИС
# -----------------------------------------

# Kafka настройки
ASSEMBLY_KAFKA_BROKERS=localhost:9092
ASSEMBLY_ORDER_PAID_TOPIC_NAME=order.paid
ASSEMBLY_ORDER_PAID_CONSUMER_GROUP_ID=assembly-group-order-paid
ASSEMBLY_ORDER_ASSEMBLED_TOPIC_NAME=order.assembled

# Логгер
ASSEMBLY_LOGGER_LEVEL=info
ASSEMBLY_LOGGER_AS_JSON=true
ASSEMBLY_LOGGER_SERVICE_NAME=assembly-service
ASSEMBLY_LOGGER_SERVICE_ENVIRONMENT=dev
ASSEMBLY_LOGGER_ENABLE_OTLP=true

# -----------------------------------------
# NOTIFICATION СЕРВИС
# -----------------------------------------

# Kafka настройки
NOTIFICATION_KAFKA_BROKERS=localhost:9092
NOTIFICATION_ORDER_PAID_TOPIC_NAME=order.paid
NOTIFICATION_ORDER_PAID_CONSUMER_GROUP_ID=notification-group-order-paid
NOTIFICATION_ORDER_ASSEMBLED_TOPIC_NAME=order.assembled
NOTIFICATION_ORDER_ASSEMBLED_CONSUMER_GROUP_ID=notification-group-order-assembled

# Telegram бот
NOTIFICATION_TELEGRAM_BOT_TOKEN=8654554130:AAG-HVWNs7KTZRu2fZxr8hLh8uuRIBkc-EM

# Логгер
NOTIFICATION_LOGGER_LEVEL=info
NOTIFICATION_LOGGER_AS_JSON=true
NOTIFICATION_LOGGER_SERVICE_NAME=notification-service
NOTIFICATION_LOGGER_SERVICE_ENVIRONMENT=dev
NOTIFICATION_LOGGER_ENABLE_OTLP=true

